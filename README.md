# SINTIAN 模板化藍圖

這個 repo 只有一頁：`index.html`，是 SINTIAN 建站生意「模板化」進度的看板，給團隊與合作方看的。

看這裡：https://sintiangoods.github.io/sintian-blueprint/

## 幾件事先說清楚

- 這頁是**鏡像**。真正的原始檔在私有的 `sintian-site` repo 的 `docs/blueprint/index.html`，
  每次那邊更新後同步過來。要改內容請改那邊，不要直接改這裡，否則下次同步會被蓋掉。
- 頁面資料全部寫在檔案裡的 `DATA` 區塊，進度百分比是現場算的，不是手寫的。
- 單一檔案、零外部資源（不引用任何 Google 字體或 CDN），大陸也打得開。
